<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr name="leftIconDrawable" format="integer" />
    <attr name="rightIconDrawable" format="integer" />

    <declare-styleable name="SearchBox">
        <attr name="hint" format="string" />
        <attr name="nextButton" format="boolean" />
    </declare-styleable>

    <declare-styleable name="NumericControl">
        <attr name="value" format="integer" />
        <attr name="min" format="integer" />
        <attr name="max" format="integer" />
    </declare-styleable>

    <declare-styleable name="DateTimePicker">
        <attr name="pickerHint" format="string" />
        <attr name="curvedEffect" format="boolean" />
        <attr name="cyclicEffect" format="boolean" />
        <attr name="atmosphericEffect" format="boolean" />
        <attr name="displayTime" format="boolean" />
        <attr name="displayDate" format="boolean" />
    </declare-styleable>

    <declare-styleable name="InteractivePanel">
        <attr name="panelHint" format="string" />
        <attr name="src" format="reference"/>
        <attr name="borderType" format="enum">
            <enum name="dotted" value="1" />
            <enum name="line" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="TermsBar">
        <attr name="titleText" format="string" />
        <attr name="termsText" format="string" />
        <attr name="privacyText" format="string" />
    </declare-styleable>

    <declare-styleable name="PricingBottomBar">
        <attr name="title" format="string" />
        <attr name="btnText" format="string"/>
        <attr name="pricing" format="string"/>
    </declare-styleable>

    <declare-styleable name="FullWidthBottomBar">
        <attr name="btnFullWidthText" format="string"/>
        <attr name="btnEnabled" format="string"/>
    </declare-styleable>

    <declare-styleable name="MobileInputMockLayout">
        <attr name="mockHint" format="string" />
    </declare-styleable>

    <declare-styleable name="MobileInputLayout">
        <attr name="inputHint" format="string" />
        <attr name="inputHintTitle" format="string" />
    </declare-styleable>

    <declare-styleable name="FacebookButton">
        <attr name="fbHint" format="string" />
        <attr name="fbEnabled" format="boolean" />
    </declare-styleable>

    <declare-styleable name="PasswordField">
        <attr name="passwordHintTitle" format="string" />
    </declare-styleable>

    <declare-styleable name="NotesStandard">
        <attr name="notesText" format="string" />
        <attr name="notesType" format="string">
            <enum name="no_button" value="1" />
            <enum name="single_button" value="2" />
            <enum name="double_button" value="3" />
            <enum name="no_icon" value="4" />
            <enum name="only_primary_text" value="5" />
        </attr>
        <attr name="notesIconDrawable" format="integer"/>
        <attr name="notesIconWidth" format="dimension"/>
        <attr name="notesIconHeight" format="dimension"/>
        <attr name="btnSingleText" format="string"/>
        <attr name="btnDoublePrimaryText" format="string"/>
        <attr name="btnDoubleSecondaryText" format="string"/>
        <attr name="notesSecondary" format="string"/>
        <attr name="showNotesSecondary" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="NotesError">
        <attr name="notesErrorText" format="string" />
        <attr name="notesErrorType" format="string">
            <enum name="no_button" value="1" />
            <enum name="single_button" value="2" />
            <enum name="double_button" value="3" />
            <enum name="no_icon" value="4" />
            <enum name="only_primary_text" value="5" />
        </attr>
        <attr name="notesErrorIconDrawable" format="integer"/>
        <attr name="notesErrorIconWidth" format="dimension"/>
        <attr name="notesErrorIconHeight" format="dimension"/>
        <attr name="btnErrorSingleText" format="string"/>
        <attr name="btnErrorDoublePrimaryText" format="string"/>
        <attr name="btnErrorDoubleSecondaryText" format="string"/>
        <attr name="notesErrorSecondary" format="string"/>
        <attr name="showNotesErrorSecondary" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="NotesInfo">
        <attr name="notesInfoText" format="string" />
        <attr name="notesInfoTitle" format="string" />
        <attr name="notesInfoIconDrawable" format="integer"/>
        <attr name="notesInfoIconWidth" format="dimension"/>
        <attr name="notesInfoIconHeight" format="dimension"/>
        <attr name="notesInfoType" format="string">
            <enum name="with_icon_and_title" value="1" />
            <enum name="with_icon" value="2" />
            <enum name="title_and_text" value="3" />
            <enum name="text_only" value="4" />
        </attr>
    </declare-styleable>

    <declare-styleable name="KaodimEditTextLayout">
        <attr name="android:hint"/>
        <attr name="errorText"/>
        <attr name="android:text"/>
        <attr name="secured" format="boolean"/>
        <attr name="capitalize" format="boolean"/>
        <attr name="iconDrawable" format="integer"/>
        <attr name="inputType" format="enum">
            <enum name="text" value="0"/>
            <enum name="number" value="1"/>
            <enum name="multiline" value="2"/>
            <enum name="password" value="3"/>
        </attr>
        <attr name="android:enabled"/>
    </declare-styleable>

    <declare-styleable name="KaodimPhoneNumberEditTextLayout">
        <attr name="errorText"/>
        <attr name="android:text"/>
        <attr name="android:enabled"/>
        <attr name="country"/>
    </declare-styleable>

    <declare-styleable name="KaodimViewTextLayout">
        <attr name="android:hint"/>
        <attr name="android:enabled"/>
        <attr name="leftIconVisibility">
            <!-- Visible on screen; the default value. -->
            <enum name="visible" value="0" />
            <!-- Not displayed, but taken into account during layout (space is left for it). -->
            <enum name="invisible" value="1" />
            <!-- Completely hidden, as if the view had not been added. -->
            <enum name="gone" value="2" />
        </attr>
        <attr name="rightIconVisibility">
            <!-- Visible on screen; the default value. -->
            <enum name="visible" value="0" />
            <!-- Not displayed, but taken into account during layout (space is left for it). -->
            <enum name="invisible" value="1" />
            <!-- Completely hidden, as if the view had not been added. -->
            <enum name="gone" value="2" />
        </attr>
        <attr name="leftIconDrawable"/>
        <attr name="rightIconDrawable"/>
    </declare-styleable>

    <declare-styleable name="SearchEditText">
        <attr name="android:hint"/>
        <attr name="cancelText" format="string"/>
        <attr name="showCancelText" format="boolean"/>
        <attr name="leftIconDrawable"/>
        <attr name="android:inputType"/>
    </declare-styleable>


    <declare-styleable name="CalendarView">

        <!-- The xml resource that is inflated and used as the day cell view, must be provided. -->
        <attr name="cv_dayViewResource" format="reference" />

        <!-- The xml resource that is inflated and used as a header for every month. -->
        <attr name="cv_monthHeaderResource" format="reference" />

        <!-- The xml resource that is inflated and used as a footer for every month. -->
        <attr name="cv_monthFooterResource" format="reference" />

        <!-- The maximum number of rows(1 to 6) to show on each month. If a month has a total of
        6 rows and maxRowCount is set to 4, there will be two appearances of that month on the
        calendar, the first one will show 4 rows and the second one will show the remaining 2 rows.
        To show a week mode calendar, set this value to 1. -->
        <attr name="cv_maxRowCount" format="integer" />

        <!-- A ViewGroup which is instantiated and used as the background for each month.
         This class must have a constructor which takes only a Context. You should
         exclude the name and constructor of this class from code obfuscation if enabled. -->
        <attr name="cv_monthViewClass" format="string" />

        <!-- Determines if dates of a month should stay in its section or can flow into another month's section.
         If true, a section can only contain dates belonging to that month, its inDates and outDates.
         if false, the dates are added continuously, irrespective of month sections.

         When this property is false, a few things behave slightly differently:
         - If inDateStyle is either `allMonths` or `firstMonth`, only the first index will contain inDates.
         - If outDateStyle is either `endOfRow` or `endOfGrid`, only the last index will contain outDates.
         - If outDateStyle is `endOfGrid`, outDates are generated for the last index until it
           satisfies the `maxRowCount` requirement. -->
        <attr name="cv_hasBoundaries" format="boolean" />

        <!--This determines the scroll direction of the the calendar. -->
        <attr name="cv_orientation" format="enum">
            <enum name="horizontal" value="0" />
            <enum name="vertical" value="1" />
        </attr>

        <!-- Determines how outDates are generated for each month on the calendar.
        If set to `endOfRow`, the calendar will generate outDates until
        it reaches the first end of a row. This means that if a month has 6 rows,
        it will display 6 rows and if a month has 5 rows, it will display 5 rows.
        If set to `endOfGrid`, the calendar will generate outDates until
        it reaches the end of a 6 x 7 grid. This means that all months will have 6 rows.
        If set to `none`, no outDates will be generated. -->
        <attr name="cv_outDateStyle" format="enum">
            <enum name="endOfRow" value="0" />
            <enum name="endOfGrid" value="1" />
            <enum name="none" value="2" />
        </attr>

        <!-- Determines how inDates are generated for each month on the calendar.
         If set to `allMonths`, the calendar will generate inDates for all months.
         If set to `firstMonth`, inDates will be generated for the first month only.
         If set to `none`, inDates will not be generated, this means that there will be
         no offset on any month. -->
        <attr name="cv_inDateStyle" format="enum">
            <enum name="allMonths" value="0" />
            <enum name="firstMonth" value="1" />
            <enum name="none" value="2" />
        </attr>

        <!-- The scrolling behavior of the calendar. If `paged`, the calendar
        will snap to the nearest month after a scroll or swipe action.
        If `continuous`, the calendar scrolls normally. -->
        <attr name="cv_scrollMode" format="enum">
            <enum name="continuous" value="0" />
            <enum name="paged" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="InfoBar">
        <attr name="primaryText" format="string" />
        <attr name="barColor" format="color"/>
        <attr name="backgroundColor" format="color"/>
        <attr name="textColor" format="color"/>
        <attr name="android:src"/>
    </declare-styleable>
</resources>